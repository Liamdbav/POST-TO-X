# Post to X

VS Code extension that generates X (Twitter) posts from your repository README.

## Usage

Open the Command Palette (`Cmd+Shift+P`) and run:

**Post to X: Generate Post from README**

## Development

```bash
npm install
npm run compile
```

Press `F5` in VS Code to launch the extension in debug mode.
